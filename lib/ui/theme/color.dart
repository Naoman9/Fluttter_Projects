// add the color that we will be using in the applciation
import 'package:flutter/material.dart';

class MainColor{
  static Color primaryColor = Colors.amber;
  static Color secondaryColor = Colors.deepOrangeAccent;
  static Color accentColor = Colors.tealAccent;
}